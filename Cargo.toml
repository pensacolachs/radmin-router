[package]
name = "radmin-router"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.10.1"
http-body-util = "0.1.3"
hyper = "1.6.0"
macros = { path = "macros" }
futures = "0.3.31"
chrono = { version = "0.4.41", optional = true }

[features]
default = []
logging = ["dep:chrono"]
